/* import { shallowMount } from '@vue/test-utils'
import AboutPage from '@/pages/sobre-nosotros/index.vue'

describe('Mounted -> About Page', () => {
    test('Truthy render', () => {
        const wrapper = shallowMount(AboutPage)
        expect(wrapper.vm).toBeTruthy()
    })
})
 */
